# Test-only dependencies. The site itself has no runtime dependencies —
# the generators use the standard library only.
pytest>=8.0

# tests/test_jekyll_excludes.py parses _config.yml. Reading Jekyll's own config with a
# real YAML parser rather than a regex is the point: the exclude list is what silently
# de-publishes pages, so the test has to see it the way the build does.
PyYAML
